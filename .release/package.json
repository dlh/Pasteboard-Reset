{
  "name": "pasteboard-reset",
  "private": true,
  "description": "Release tooling for Pasteboard Reset.",
  "scripts": {
    "changelog:backfill": "node -e \"require('fs').writeFileSync('.changelog-context.json', JSON.stringify({version: require('fs').readFileSync('../VERSION', 'utf8').trim()}))\" && conventional-changelog -p angular -r 0 -c .changelog-context.json -i ../CHANGELOG.md -s && node -e \"const fs = require('fs'); fs.writeFileSync('../CHANGELOG.md', fs.readFileSync('../CHANGELOG.md', 'utf8').trimStart())\" && rm .changelog-context.json",
    "release": "semantic-release --extends ./semantic-release.config.cjs",
    "release:dry-run": "semantic-release --extends ./semantic-release.config.cjs --dry-run --no-ci --plugins @semantic-release/commit-analyzer @semantic-release/release-notes-generator"
  },
  "engines": {
    "node": "^22.14.0 || >=24.10.0"
  },
  "devDependencies": {
    "@semantic-release/changelog": "7.0.0",
    "@semantic-release/exec": "7.1.0",
    "@semantic-release/git": "11.0.1",
    "conventional-changelog": "8.1.3",
    "conventional-changelog-angular": "9.2.1",
    "semantic-release": "25.0.8"
  }
}
